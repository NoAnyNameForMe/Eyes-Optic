version: "3.8"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GEMINI_API_KEY: ${GEMINI_API_KEY}
    ports:
      - "8080:80"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    restart: unless-stopped